-- Create database if it doesn't exist
CREATE DATABASE lead_management;

-- Connect to the database and run the schema
\c lead_management;

-- Run the schema from your existing schema.sql file
-- This should be run separately after connecting to the database
